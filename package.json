{
  "name": "cinepoporopo",
  "private": true,
  "version": "2.4.0",
  "description": "POPOROPO — free HD streaming site (static, GitHub Pages)",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
