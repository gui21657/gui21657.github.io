{
  "name": "cinepoporopo",
  "private": true,
  "version": "2.7.3",
  "description": "POPOROPO — free HD streaming site (static, GitHub Pages)",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
